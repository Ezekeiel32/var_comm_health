# Core dependencies
# Python 3.9+ required

# LangGraph and LangChain
langgraph>=1.0.0
langgraph-cli>=0.2.0  # Required for 'langgraph dev' command
langchain-core>=0.3.0
langchain-nvidia-ai-endpoints>=0.1.0

# Pydantic (data validation)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Transitive dependencies (explicitly listed for better compatibility)
# These are typically installed automatically, but listing them helps on systems without Anaconda
langgraph-checkpoint>=0.2.0
langgraph-prebuilt>=0.2.0
langgraph-sdk>=0.2.0
langsmith>=0.1.0
xxhash>=3.0.0
aiohttp>=3.8.0
filetype>=1.2.0
jsonpatch>=1.32
packaging>=21.0
PyYAML>=6.0
tenacity>=8.0.0
typing-extensions>=4.5.0
click>=8.0.0  # Required by langgraph-cli

